在C:\Users\管晨皓\Desktop\temp\standard\mine\outputs\experiment\<sample>_421下面，有30857和26525两个，分割结果都是好的。现在的问题是base_move_preview.html能精确分割base和move，但存在move总是缺少voxel的情况。第一仔细思考这个是stageb的工作，还是只是一个可视化而已，后续由stagec重新处理。第二、下一步应该做什么，基于当前的结果重新思考stageC的流程，目标是什么，如何优化的处理，与stageb的输出结果形成联动。

回答 Q1: base_move_preview.html 的本质
查一下代码就能确认：

# stage_b_scar.py 末尾 viz 部分（目前的代码）
bmp_base, bmp_move = joint_free_split(
    binary, sigma_b=0.25, sigma_m=0.15, tau_b=0.05, tau_m=0.05,
)
save_two_masks_html(..., os.path.join(viz_dir, "base_move_preview.html"), ...)
这里调用的 joint_free_split 实际是 Stage C 的 C.1 函数（在 pipelines/sajo/anchors.py），它用的是纯 跨 state 方差 + 占据 mean 的公式做 soft 分类。Stage B 这里只是 preview——把 Stage C 的 C.1 提前调一次给你看"如果直接进 Stage C，C.1 会把 base/move 分成什么样"。

所以 move 缺 voxel 不是 Stage B 的问题，是 joint_free_split 本身的问题——它用的是纯方差分类，正是我们 v4.2/v4.3 里花大力气解决的"长抽屉 trajectory middle 方差 0 被误判为 base"这个根本病。

两种解释都成立，但意义不同
形式上：base_move_preview 确实只是 viz，Stage C 后续会 EM 迭代 + 轴 inference，能部分修
本质上：Stage C 的 C.1 方差 split 本身有先天 bug，EM 迭代能不能修得回看不同 case。long-drawer prismatic case 里方差 split 初始化极差，EM 可能收敛到错局部最优
所以 Q1 的真正回答是：Stage C 需要升级，不要让它从方差 split 起步，而是从 Stage B 已经算好的 M_attn 起步。这是 v4.3 给我们的"免费午餐"——M_attn 已经是一个 training-free 的 base/move 语义判别器，Stage C 直接用就是。

回答 Q2: Stage C 下一步的设计
先明确 Stage C 的真正职责（Stage B v4.3 改变了职责边界）
之前 Stage C 要做的（design.md §4 的 SAJO）：

C.1 base/move split （方差 + mean）
C.2 contact anchors（for revolute）
C.3 初始轴
C.4 EM 迭代
C.5 BIC revolute vs prismatic
现在 Stage B v4.3 已经干掉的：

base/move 的几何+语义双判别（通过 M_attn_64 × P_base 的交集）—— 比方差 split 干净得多
Stage C 仍然必须做的（Stage B 无法做）：

关节轴推断 — 具体的 3D 线 / 平面
关节类型分类 — revolute vs prismatic
每 state 的变换量 — 角度或距离
URDF 装配 — 基座 mesh + 动部件 mesh + 关节描述
Stage C 的新定位："给定一个语义-几何双判别的 base/move 先验，推断关节轴 + 类型 + 变换参数"。前一半是 Stage B 的活，Stage C 集中攻后一半。

Stage C v2 提议
═══ Stage C v2 ═══
输入:
  - O_stack (K, 64³)            ← Stage B v4.3 输出占据
  - M_attn_64 (64³)             ← Stage B v4.3 的 M_attn 上采样（重点！）
  - z_final (K, 8, 16³) 可选    ← 如果 Stage C 要回看 latent features
─────────────────────
C.0 [NEW] Semantic-prior-init: 用 M_attn 初始化 base/move
─────────────────────
  # 现在的 C.1 joint_free_split 改成二元融合:
  P_var = cross_state_variance(O_stack)           # 64³ 跨 state 方差
  P_mean = O_stack.mean(dim=0)                    # 64³ 跨 state 均值
  
  # M_attn 作为语义先验
  # 柜体体素: 方差低 + M_attn 高 + mean 高 → P_base 高
  # 移动体素: 方差高 OR M_attn 低 → P_move 高
  # 空气:     mean 低, 两个都 0
  P_base = M_attn_64 * sigmoid((P_mean - 0.5)/τ)  # 几何 AND 语义
  P_move = (1 - M_attn_64) * sigmoid((P_mean - 0.3)/τ)  # 语义非一致 AND 有内容
  
  # 此时 long-drawer trajectory middle voxel:
  #   几何(P_var) 低、P_mean 高 → 旧 C.1 错判 base
  #   M_attn 低（v4.3 semantic signal） → P_base 被 M_attn clip 到 0
  #   P_move 高 → 正确归 move ✓
─────────────────────
C.1 (原 C.2) Contact anchors + C.3 init axis
─────────────────────
  # 用新的 P_move 做 26-connectivity 连通分量
  # Revolute: 找 base/move 接触面 → 初始轴 (2 anchors → 直线)
  # Prismatic: 对齐 K 个 P_move 的质心 → 平移向量
─────────────────────
C.2 (原 C.4) EM 迭代 with axis-aware refinement
─────────────────────
  # 关键改动: 每次 E-step 同时用方差信号 + M_attn 信号 + axis consistency
  # axis consistency: 给定当前轴, canonical 坐标下 move voxel 应该对齐
  #   对齐好 → 是真 move
  #   对齐差 → 可能是 base 误判为 move, 或 move 但 axis 错
  
  for epoch in range(N_outer):
      # E-step: 更新 P_base/P_move
      P_move_new = combine(P_var, 1-M_attn, axis_consistency_score)
      
      # M-step: 给定 P_move, 重拟合轴 + 关节 params
      axis_new = refit_from_move_voxels(P_move_new, O_stack)
      joint_params_new = ...
─────────────────────
C.3 (原 C.5) BIC revolute vs prismatic
─────────────────────
  # dual-fit, 同时拟合 rev 和 pris, BIC 选
  # 对于 long-drawer case, 只要 P_move 干净了, BIC 就能选对 prismatic
─────────────────────
输出:
  - joint_type ∈ {revolute, prismatic}
  - axis (origin, direction)
  - joint_params (angle/distance per state)
  - M_base, M_move (refined 64³ mask)
  - URDF 组件
关键创新（AAAI story 角度）
"M_attn: The Semantic Interface Between Stage B and Stage C"

Stage B 的 DiT self-attention feature agreement 是一个 zero-cost 语义信号（不额外训练）
传统方差 split 对"长抽屉 trajectory overlap"不 work——这是 joint recovery 领域的 pathological case
我们把这个语义信号作为 Stage B→C 的 principled interface：Stage B 不仅产生 occupancy，还产生一个"这些 voxel 是语义一致的 base"的 soft label
Stage C 的 EM 不再从 scratch 重建 base/move，而是refine a semantically-informed prior
这给 AAAI 一个清晰的 pipeline architecture 贡献点：跨模块的语义信号传递，而不是每个 stage 独立重做同样的判别。

具体要做的事（按优先级）
即时可做（低成本高 ROI）：

Stage B 导出 M_attn_64 到 stage_b 产物（持久化，供 Stage C 读）
# 现在 M_attn_64.npy 已经存了（在 viz/bmcsa/）
# 只要把它也 copy 到 stage_b/M_attn_64.npy 作官方产物
更新 base_move_preview viz：让它用 M_attn 加权的 split，不要用纯方差
审查 pipelines/sajo/anchors.py::joint_free_split：看能不能接受 M_attn 作为可选 prior 参数
中期（重新设计 Stage C）：

Stage C v2 spec 写作（先 brainstorm，再写 spec）
实现 C.0 语义 prior init（~30 行代码）
EM 的 E-step 融入 axis consistency score（需要仔细设计）
长期（paper 实验）：

消融：
纯方差 split (旧 Stage C) vs M_attn-initialized split (新 Stage C) 在 PARIS/PartNet-Mobility 的 joint axis 推断精度
分别看 prismatic / revolute / 长抽屉 sub-case 的精度
跨数据集泛化：v4.3 在 YOUR 数据集上好，但 M_attn 阈值 (threshold=0.7) 在别的数据集上能不能直接用？需要 sensitivity study